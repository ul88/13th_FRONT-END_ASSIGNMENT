// components/NumButton.js
/*import React from "react";
import styles from "./NumButton.module.css";

function NumButton({ value, onClick }) {
  return (
    <div className={styles.num_buttons} onClick={() => onClick(value)}>
      {value}
    </div>
  );
}

export default NumButton;*/
