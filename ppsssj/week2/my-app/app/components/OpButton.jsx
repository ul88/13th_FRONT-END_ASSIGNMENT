// components/OpButton.js
/*import React from "react";
import styles from "./page.module.css";
function OpButton({ value, onClick }) {
  return (
    <div className={styles.op_buttons} onClick={() => onClick(value)}>
      {value}
    </div>
  );
}

export default OpButton;
*/